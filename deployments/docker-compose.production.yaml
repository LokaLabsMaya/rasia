# Production deployment — Tailnet access via Traefik tailscale entrypoint
services:
  rasia:
    image: ${GITHUB_IMAGE:-ghcr.io/lokalabsmaya/rasia}:${IMAGE_TAG:-latest}
    container_name: rasia-prod
    restart: unless-stopped
    command: ["http", "--config", "/rasia/configs/files/default.yaml"]
    volumes:
      - /home/lokalabs/data/infra/rasia/rasia-prod:/rasia/secrets
      - ./configs/files/default.yaml:/rasia/configs/files/default.yaml:ro
    networks:
      - production
    labels:
      - traefik.enable=true
      - traefik.docker.network=production
      - traefik.http.routers.rasia.rule=Host(`${RASIA_HOST:-rasia.lokalabs.buzz}`)
      - traefik.http.routers.rasia.entrypoints=tailscale
      - traefik.http.routers.rasia.tls=true
      - traefik.http.routers.rasia.tls.certresolver=letsencrypt
      - traefik.http.services.rasia.loadbalancer.server.port=8000
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "wget -qO- http://127.0.0.1:8000/ > /dev/null 2>&1 || exit 1",
        ]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: "3"

networks:
  production:
    name: production
    external: true
