name: Pipeline

on:
  push:
    branches: [development, main]
    tags:
      - "v*"
  pull_request:

jobs:
  ci:
    uses: lokalabsmaya/infrastructure/.github/workflows/ci-golang.yml@main
    permissions:
      contents: read
      packages: write
      actions: write
    secrets: inherit
    with:
      go_version: "1.25.0"
      dockerfile: deployments/build/Dockerfile
      context: .
      release_branch: development
      cgo_enabled: true
      build_args: |
        appName=rasia
        appPath=.

  cd-development:
    needs: ci
    if: github.ref == 'refs/heads/development'
    uses: lokalabsmaya/infrastructure/.github/workflows/cd-golang.yml@main
    secrets:
      SSH_HOST: ${{ secrets.SSH_HOST }}
      SSH_USERNAME: ${{ secrets.SSH_USERNAME }}
      SSH_PASSWORD: ${{ secrets.SSH_PASSWORD }}
    with:
      service_path: rasia/rasia-dev
      compose_file: deployments/docker-compose.development.yaml
      image_tag: development
      environment: infra
      use_rasia: false
      use_tailscale: true

  cd-production:
    needs: ci
    if: startsWith(github.ref, 'refs/tags/v')
    uses: lokalabsmaya/infrastructure/.github/workflows/cd-golang.yml@main
    secrets:
      SSH_HOST: ${{ secrets.SSH_HOST }}
      SSH_USERNAME: ${{ secrets.SSH_USERNAME }}
      SSH_PASSWORD: ${{ secrets.SSH_PASSWORD }}
    with:
      service_path: rasia/rasia-prod
      compose_file: deployments/docker-compose.production.yaml
      image_tag: ${{ github.ref_name }}
      environment: infra
      use_rasia: false
      use_tailscale: true